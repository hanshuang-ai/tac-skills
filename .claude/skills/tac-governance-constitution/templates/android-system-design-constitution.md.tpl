# Android 工程规范专项宪章

## 适用范围

本宪章适用于 Android 应用的系统设计、架构设计、模块划分、分层边界、依赖治理、资源治理、差异化处理、质量验证和协作评审。

凡是会影响项目整体技术结构、公共基础设施、模块边界、长期维护成本或运行质量的变更，都必须遵守本宪章。具体业务领域、产品形态、设备专项、模块清单和验收标准应写入项目级 Spec Kit 产物或业务设计文档，不得写入本通用专项宪章。

若本宪章与项目主宪章冲突，以项目主宪章为准；若项目级设计文档与本宪章冲突，业务事实以项目级设计文档为准，通用工程规则以本宪章为准，并在评审中记录裁剪原因。

## 1. 文档目标与使用对象

本宪章用于统一 Android 应用研发的工程规范基线，服务以下角色：

- 工程规范负责人：制定架构边界、模块基线和选型规则。
- Android 开发者：依据统一技术栈、分层边界和模块规则开展实现。
- 评审人：检查方案是否符合长期规范、质量要求和目标运行环境约束。
- 流程维护者：将架构基线持续沉淀到 Spec Kit 产物与团队协作流程中。

## 2. 架构目标与设计原则

### 2.1 总体目标

- 高内聚、低耦合。
- 模块职责清晰，便于长期演进。
- 默认支持稳定性、兼容性、可测试性和恢复能力。
- 支持 AI 辅助开发、多人协作评审和可追溯交付。

### 2.2 基础原则

- 优先采用 Kotlin 与 Gradle Kotlin DSL。
- UI 技术栈应在 Jetpack Compose 与 Android Views(XML) 之间按项目阶段、团队能力、存量代码和目标设备形态选择；新项目优先评估 Compose，存量 Views 项目可继续使用 Views + ViewBinding。
- 混合使用 Compose 与 Views 时，必须明确边界、互操作方式、状态归属和迁移策略。
- 架构优先采用 MVVM、MVI、Clean Architecture 或等价职责分离思想；不得为了形式化分层制造无实际收益的空转代码。
- 坚持 SSOT、UDF 和数据模型驱动，保证状态源唯一与数据流方向清晰。
- 优先复用 Jetpack 与官方能力；外部库引入必须说明收益、替代方案、维护成本和退出策略。
- 优先通过稳定契约通信，禁止直接依赖跨层或跨模块实现细节。

### 2.3 七大设计原则

- 单一职责优先：Activity、Fragment、Composable、ViewModel、UseCase、Repository、DataSource 各自只承载本层职责，禁止把页面逻辑、业务规则、数据访问和系统适配混写在同一类中。
- 面向接口与依赖倒置：上层依赖抽象而非依赖实现，Repository、系统能力适配器和跨模块能力默认通过接口暴露，便于替换、测试和解耦。
- 开闭优先于分支扩张：新增能力优先通过扩展点、策略对象、配置注入和受控抽象扩展，避免在既有核心流程中堆叠大量 `if/else` 或 `when` 分支。
- 生命周期与状态一致性：界面状态订阅、异步任务和资源释放必须与 Lifecycle 对齐；对外暴露的 UI 状态应以不可变数据模型表达，状态变更通过明确事件驱动。
- 可测试性前置：业务规则、状态转换、数据映射和系统适配边界必须设计成可单测、可替身、可注入，禁止把关键逻辑绑定在难以测试的 Android 组件细节中。
- 最小惊讶原则：同类页面、同类状态和同类错误处理应保持一致交互语义、命名方式和返回结构，避免局部特例破坏全局可理解性。
- 失败优先设计：弱网、空数据、超时、权限拒绝、外部能力不可用、系统回收和进程重建等异常路径必须作为默认设计对象，而不是在实现末期补丁式处理。

### 2.4 设计模式使用约束

- 设计模式不是默认必须项，而是用于落实设计原则的受控手段。
- 采用策略、工厂、适配器、观察者、装饰器等设计模式时，必须说明它服务于哪条设计原则、为何优于更简单方案，以及是否会引入额外理解成本或边界复杂度。
- 禁止为了“看起来规范”而机械套用设计模式；若模式没有实质降低耦合、复杂度或变更成本，应优先使用更直接的实现。

## 3. 模块化治理

### 3.1 模块化基线

模块化是中大型 Android 工程的推荐治理方向，但不得为形式化拆分牺牲可理解性。项目可根据规模、稳定边界和治理成本选择单模块或多模块形态；无论是否拆成 Gradle 模块，都必须保持清晰的职责边界、依赖方向和可测试结构。

### 3.2 推荐模块类型

```text
app/
feature-*/
core-*/
platform-*/
```

- `app`：负责应用启动、进程级初始化、DI 装配、导航装配和模块聚合；不得承载业务规则、接口访问或数据存储实现。
- `feature-*`：负责单一稳定业务域的页面编排、状态管理、UseCase 组合与业务契约；不得跨业务域混装多个不稳定场景。
- `core-*`：负责多个业务域共享的稳定能力，例如 UI 基础组件、公共模型、网络、存储、日志和通用工具。
- `platform-*`：负责系统服务、厂商能力、安装服务、配置读取等平台适配与系统能力隔离；不得反向依赖具体业务实现。

### 3.3 模块准入门槛

新增独立模块至少满足以下条件之一：

- 对应稳定业务域或一级功能域，且预计会持续独立演进。
- 被多个业务域复用，抽离后能显著降低重复实现或耦合。
- 需要显式隔离平台或基础设施职责，避免业务层直接依赖系统细节。
- 需要单独治理依赖、资源、发布节奏或测试边界。

以下情形禁止创建新模块：

- 仅为了单页面、单弹窗、单 Adapter 或单次活动页整理目录。
- 仅有临时复用、短期复用或尚未稳定命名的能力。
- 只是把同一业务链路机械拆成多个薄层模块，却没有稳定契约或清晰边界。
- 新模块引入后只增加依赖跳转层，却没有降低耦合、复用或治理成本。
- 调用方尚未稳定、真实复用边界尚未形成，却因为预设未来场景提前拆出独立模块。

### 3.4 已拆模块与未拆模块的同构结构

- 无论能力是否已拆成独立 Gradle 模块，只要构成稳定边界，都必须采用一致的 `ui/domain/data` 或等价内部结构。
- `ui/` 负责页面、状态、事件、导航触发和界面组装，不得直接访问数据实现。
- `domain/` 负责 UseCase、领域规则、跨仓协调和稳定业务语义；简单场景可不独立成目录，但职责必须显式建模。
- `data/` 负责 Repository、DataSource、缓存、接口模型转换和平台能力接入。
- 对外暴露能力时，优先通过稳定契约、接口、导航契约或共享模型，而非暴露内部实现类。
- 若某能力尚未拆成独立模块，必须在宿主模块内用包结构模拟未来边界，并按相同依赖方向组织代码。

## 4. 分层架构与依赖方向

### 4.1 标准分层

- UI Layer：负责界面渲染、输入接收、状态订阅、一次性 UI 事件处理与导航触发。
- Domain Layer：负责 UseCase、领域规则、业务编排与可复用业务逻辑；复杂或复用场景必须显式建模，简单场景可按职责内聚在 ViewModel 或 Repository 边界内，但不得污染 UI 与 Data 的职责。
- Data Layer：负责 Repository、DataSource、缓存、接口访问、持久化访问和数据转换。
- Platform Layer：负责系统服务、权限、厂商差异、设备能力和外部 SDK 适配。

### 4.2 调用链路

标准调用链路优先保持为：

`View/Composable -> ViewModel -> UseCase -> Repository -> DataSource`

简单场景允许省略 UseCase，但必须保持：

`View/Composable -> ViewModel -> Repository -> DataSource`

### 4.3 强约束

- 禁止 View、Fragment、Activity 或 Composable 直接访问 Repository、DataSource、数据库、网络接口或系统适配实现。
- 禁止 UI 层承载业务规则、数据持久化逻辑或平台差异判断。
- 禁止跨模块直接调用实现类。
- 模块通信必须通过接口、UseCase、导航契约或稳定数据模型完成。
- `app` 只允许依赖 `feature-*`、`core-*`、`platform-*` 的公开能力，不得成为业务逻辑的兜底容器。
- `feature-*` 可依赖 `core-*` 与 `platform-*` 的公开契约，但默认不得彼此直接依赖实现。
- `core-*` 之间仅允许围绕明确公共能力形成受控依赖，禁止重新演化为隐式“大公共模块”。
- `platform-*` 不得依赖 `feature-*`，避免平台适配反向绑定业务流程。

### 4.4 设计表达最小要求

- 每个系统设计方案必须明确输入、输出、边界条件和异常处理，禁止只描述主流程而省略失败路径。
- 每个模块或层都必须写清职责边界，说明“负责什么”和“不负责什么”。
- 每条关键依赖关系都必须可追溯，至少说明依赖方向、依赖原因和解耦方式。
- 不允许使用“按需处理”“后续补充”“视情况决定”这类无法执行或无法评审的模糊描述作为最终设计结论。

## 5. 状态建模与数据模型边界

- 系统设计必须区分 UI 状态、领域模型、接口模型和持久化模型，禁止一个数据结构同时承担多层职责。
- UI 状态必须围绕页面渲染、交互反馈和恢复能力建模，不得直接暴露底层接口字段或数据库结构。
- 领域模型必须表达稳定业务语义，避免直接绑定接口字段命名、平台对象或临时展示拼装结果。
- 接口模型与持久化模型必须通过显式映射进入领域层或 UI 层，禁止跨层透传导致模型污染。
- 关键状态流转必须写清状态集合、触发事件、终止条件和异常分支，避免隐式状态切换。
- UI 对外暴露状态优先使用不可变数据结构；一次性事件必须与可恢复状态区分，避免配置变更或进程重建后重复消费。

## 6. 并发、线程与生命周期

- 系统设计必须明确主线程、IO 线程和计算密集线程各自承载的职责，禁止在主线程执行阻塞式 I/O 或重计算。
- 异步任务必须说明生命周期归属、取消时机、重试策略和并发边界，避免出现悬挂任务或重复执行。
- Coroutine、Flow 或其他异步链路必须说明背压、去重、超时和错误传播策略。
- UI 层收集状态必须与 Lifecycle 对齐；Compose 与 Views 场景均不得使用脱离生命周期的长期订阅。
- 涉及多数据源聚合、列表刷新、轮询同步、下载上传或后台补偿时，必须显式说明并发控制方式。
- 后台任务优先评估 WorkManager、前台服务或系统调度能力的适用性，并明确恢复、取消和幂等策略。

## 7. 技术选型治理

每个技术域都必须从学习成本、官方兼容性、目标设备适配性、可测试性、治理复杂度和长期维护成本进行评估，并给出默认推荐与受控替代范围。

| 技术域 | 推荐基线 | 受控替代范围 | 约束说明 |
| ------ | -------- | ------------ | -------- |
| 语言 | Kotlin | Java（仅限存量兼容或平台约束） | 新增业务代码优先 Kotlin |
| 界面构建 | Jetpack Compose 或 Android Views + ViewBinding | Compose/View 混合 | 新项目优先评估 Compose；存量 Views 项目允许继续治理 |
| 生命周期与状态 | Lifecycle + ViewModel + SavedStateHandle + StateFlow | LiveData（存量兼容） | 新方案优先 StateFlow，保持单向状态流 |
| 依赖注入 | Hilt | Koin、手动 DI | 替代方案必须说明原因、边界和测试影响 |
| 网络通信 | Retrofit + OkHttp | Ktor Client、Volley | 必须统一序列化、超时、拦截器和错误语义 |
| 数据序列化 | kotlinx.serialization | Moshi、Gson | 新方案优先 Kotlin 原生友好方案 |
| 数据存储 | Room + DataStore | SQLiteOpenHelper、SharedPreferences、MMKV | 结构化数据优先 Room，轻量配置优先 DataStore |
| 异步处理 | Kotlin Coroutines + Flow | RxJava（存量兼容） | 新增模块禁止无评审引入新的 Rx 链路 |
| 导航与结果回传 | Navigation Component + Activity Result API | 显式 Intent 协议、项目内导航抽象 | 禁止 EventBus 类全局隐式总线承载核心流程 |
| 后台任务 | WorkManager | 前台服务或自管调度 | 需说明系统限制、恢复策略和资源边界 |
| 列表分页 | Paging | 手工分页实现 | 分页场景优先官方生态能力；手工实现需说明收益 |
| 日志监控 | Timber 或统一日志封装 | Android Log、厂商监控 SDK | 业务层不得散落原生日志调用 |
| 测试验证 | JUnit + MockK + Espresso/Compose UI Test | Mockito、Robolectric、Maestro | 测试框架需与仓库现状和设备场景兼容 |
| 构建发布 | Gradle Kotlin DSL + Version Catalog + AGP | Groovy DSL（存量兼容） | 依赖版本统一收口管理 |
| 差异化管理 | BuildConfig + resValue + Properties + SourceSets + DI 抽象 | Product Flavors | 禁止在业务代码里写设备、渠道或品牌 `if/else` |

## 8. 依赖、构建与资源治理

### 8.1 依赖治理

- 统一使用 `libs.versions.toml` 或等价 Version Catalog 管理依赖版本。
- 新增模块、迁移模块或新增三方依赖时，必须同步更新 Version Catalog，不得在模块级 `build.gradle.kts` 中写死版本号。
- 根级插件版本也必须通过 Version Catalog 或统一位置声明，不得在多个 Gradle 脚本中重复维护插件版本。
- 模块级 `build.gradle.kts` 只允许引用 `libs.xxx`、`libs.plugins.xxx` 或 `project(...)` 等已收口定义，禁止直接写 `"group:name:version"` 形式的新依赖。
- 禁止模块私自声明同类库的不同版本。
- 公共三方能力必须收敛到公共模块或统一封装。
- 外部库引入时必须说明用途、替代方案、维护风险和退出策略。
- 引入新模块时必须同步说明为何不能留在现有模块内，以及新增模块是否满足准入门槛。

### 8.2 构建与差异化治理

- 差异化需求必须先区分配置差异、功能差异、资源差异和平台能力差异。
- 优先通过 `BuildConfig`、`resValue`、`Properties`、`SourceSets`、接口抽象和依赖注入处理差异。
- `Product Flavors` 仅在复杂版本矩阵确有必要时引入。
- 禁止在业务代码中通过 `if/else` 直接区分设备形态、渠道、厂商或品牌。

### 8.3 资源与混淆治理

- 多模块项目中的每个 Android Library 模块必须设置 `resourcePrefix`；单模块项目也应采用可追溯的资源命名前缀。
- 资源命名应可追溯到模块、能力或业务域，禁止无语义缩写和同义混用。
- `SourceSets` 可用于多设备、多品牌、多渠道差异资源管理。
- 混淆规则按业务模块、模块关系和第三方依赖分组治理，禁止把所有规则无差别堆入单一文件。

## 9. 接口契约与系统能力接入

- 模块间能力、层间调用和系统能力接入必须通过稳定契约表达，契约至少包含输入、输出、错误语义和版本边界。
- 系统能力适配层必须屏蔽平台细节、厂商差异和权限细节，不得让 UI 或业务规则直接耦合底层实现。
- 外部接口、SDK、系统服务或三方能力的接入必须明确超时、失败、空结果、重试和回退策略。
- 新增契约时必须说明向后兼容方式，避免无计划地破坏既有调用方。
- 权限、账号、定位、蓝牙、通知、安装、存储、媒体、后台执行等敏感能力必须说明授权边界、失败反馈和降级路径。

## 10. 测试策略

- 系统设计必须同步考虑测试策略，至少说明单元测试、集成测试、UI 测试或设备验证应覆盖哪些关键模块、状态转换和异常路径。
- 关键业务规则、状态机转换、数据映射、Repository 协调逻辑和系统适配边界必须优先设计为可单元测试对象。
- 涉及页面状态、导航链路、资源切换、权限交互、系统能力联动或目标设备运行限制的方案，必须评估是否需要集成测试、UI 测试或设备验证。
- 测试设计必须覆盖正常路径、边界条件和失败路径，不允许只验证成功场景。
- 若某类测试因环境、设备、平台能力或阶段限制暂未执行，设计与交付说明中必须明确未覆盖范围、风险和补偿措施。

## 11. 可观测性、安全与隐私

- 关键业务链路必须具备可观测性设计，至少说明日志、埋点、监控或诊断信息中的一种主路径。
- 日志事件、状态事件和错误事件必须可关联到模块、场景、操作类型和结果状态，便于问题追踪。
- 可观测性设计必须区分开发调试信息、运行诊断信息和业务分析信息，避免同一通道承载所有用途。
- 任何观测方案都不得泄露账号、令牌、设备敏感信息或用户隐私内容。
- 网络、存储、日志、剪贴板、截图、导出文件和第三方 SDK 接入必须评估安全与隐私边界。

## 12. 性能、资源与恢复能力

- 系统设计必须说明关键页面与关键链路的性能敏感点，例如启动、首屏、列表滚动、图片加载、状态恢复和后台任务恢复。
- 图片、缓存、分页、预加载和本地存储策略必须说明收益、成本和失效条件，避免隐式资源膨胀。
- 内存、磁盘、网络和后台任务使用必须有边界意识，禁止为追求局部体验牺牲长期稳定性。
- 弱网、离线、系统回收、进程重建、配置变更和外部能力暂时不可用时，必须具备可恢复路径或降级路径。
- 大屏、折叠屏、TV、车载、Wear OS、XR、嵌入式设备等设备形态差异应在项目级规范中补充，本宪章只要求方案显式识别目标设备约束并给出工程处理方式。

## 13. 架构演进与兼容性

- 新增架构模式、公共模块、基础设施能力或关键三方库时，必须说明引入原因、替代方案和迁移影响。
- 涉及存量模块改造时，必须说明兼容旧实现的策略、迁移边界和回滚方式。
- 架构演进应优先渐进式收敛，避免一次性大改导致长期并行双轨或无法落地的理想化分层。
- 临时兼容方案必须声明生效范围和退出条件，禁止把阶段性妥协沉淀为永久架构。

## 14. AI 输入与协作要求

AI 生成 Android 代码前至少应准备以下输入：

1. UI Spec：组件树、页面结构、状态展示和导航入口。
2. Interaction Spec：交互状态、事件、状态机和失败反馈。
3. Data Model：UI 状态、领域模型、接口模型与持久化模型。
4. Constraints：架构、技术栈、模块边界、目标设备、差异化约束和质量闸门。

AI 协作还必须满足以下行为约束：

- 不允许编造架构事实、依赖关系、系统能力或已有实现；无法确认时必须显式标记为待确认。
- 必须基于当前仓库上下文、已引用规范和输入文档进行推理，禁止脱离现有约束给出看似合理但不可落地的方案。
- 对关键设计决策必须解释原因，至少说明为什么这样设计、替代方案为何不采用、以及对复杂度与维护性的影响。
- AI 输出的系统设计内容必须显式区分“已确认规则”“推断结论”“待确认事项”，禁止把推断包装成既定事实。

## 15. 交付与评审要求

- `spec.md`、`plan.md`、`tasks.md` 必须显式引用本宪章。
- 任何提高复杂度的设计都必须写明为何不能采用更简单方案。
- 评审时必须检查模块化治理、分层边界、输入输出与异常路径、模块职责、依赖治理、技术栈选择、设计原则和目标设备约束是否完整。
- 评审时必须额外检查测试策略是否覆盖关键规则、关键状态、关键依赖和主要失败路径，以及未覆盖项是否写明原因。
- 业务相关规则应沉淀到项目级业务设计、软件设计或验收标准中，不得与本通用专项宪章混写。
- 新增模块必须说明准入依据；未拆模块必须说明是否已按未来边界形成同构内部结构。

### 15.1 评审检查清单

- 是否明确系统目标、输入、输出、边界条件和异常处理。
- 是否明确 UI、Domain、Data、Platform 的职责边界和调用方向。
- 是否明确状态建模、数据模型分层和关键状态流转。
- 是否说明关键依赖、契约边界、系统能力接入方式和解耦策略。
- 是否说明模块准入依据、禁止拆分情形和模块数量收敛思路。
- 是否说明已拆模块与未拆模块的 `ui/domain/data` 同构结构和迁移方式。
- 是否说明线程模型、并发控制、取消时机和错误传播方式。
- 是否覆盖测试策略、失败路径、未验证项和补偿措施。
- 是否覆盖可观测性、安全隐私、性能敏感点、资源边界和恢复路径。
- 是否说明架构演进、兼容策略、迁移边界和回滚方式。
- 是否存在业务规则、设备专项规则或项目路径误写入通用专项宪章的情况。

## 参考来源

- Android Developers: Guide to app architecture
- Android Developers: Architecture recommendations
- Android Developers: Jetpack Compose documentation
- Android Developers: Hilt dependency injection documentation
- Android Developers: Kotlin coroutines and Flow with Lifecycle documentation
- 项目主宪章 `.specify/memory/constitution.md`
