# Android Kotlin 编码规范专项宪章

## 适用范围

本宪章适用于 Android 项目中的 Kotlin 源码、Kotlin 测试代码、Gradle Kotlin DSL 脚本、Kotlin 注释、命名、文件布局、格式化、语言惯用法和代码评审。

本宪章以 Google Android Kotlin Style Guide 为硬性基线，以 Kotlin 官方编码规范为补充。若两者存在差异，Android 应用代码优先遵循 Google Android Kotlin Style Guide；非 Android 共享 Kotlin 代码可在项目级规范中说明采用 Kotlin 官方编码规范的裁剪边界。

具体业务命名、包名根命名空间、模块职责和测试命令由项目级主宪章、工程规范专项宪章和 Spec Kit 产物维护，不得写入本通用编码规范专项宪章。

## 1. 基础原则

- 代码首先服务于可读性、可维护性和可评审性，而不是个人风格。
- 格式化规则必须可由 IDE、ktlint、detekt 或等价工具稳定执行；无法自动化的规则必须能被评审人明确判断。
- 新增 Kotlin 代码默认遵循不可变优先、空安全优先、表达清晰优先和最小可见性原则。
- 不得为了“看起来 Kotlin 化”滥用高阶函数、操作符重载、DSL、反射或内联泛型。
- 任何局部风格若与本宪章冲突，必须在项目级规范中说明原因、范围和退出条件。

## 2. 源文件与文件布局

### 2.1 文件编码与命名

- 所有 Kotlin 源文件必须使用 UTF-8 编码。
- 单一顶级类、接口、枚举或对象文件，文件名必须与该顶级类型名称一致，并使用 `.kt` 扩展名。
- 包含多个顶级声明的文件，文件名必须能描述文件主题，使用 PascalCase 或符合 Kotlin 官方建议的可读命名。
- 不相关的顶级声明不得堆入同一文件；扩展函数、类型别名、常量和工具函数必须围绕单一主题组织。

### 2.2 文件结构顺序

`.kt` 文件按以下顺序组织：

1. 版权或许可头（如项目要求）。
2. 文件级注解。
3. `package` 语句。
4. `import` 语句。
5. 顶级声明。

各部分之间保留一个空白行。版权或许可头不得使用 KDoc 样式；若项目不要求版权头，不得由 AI 自动添加。

### 2.3 Package 与 Import

- `package` 语句不换行，不受 100 字符行宽限制。
- package 名称全部小写，连续单词直接连接，不使用下划线。
- `import` 语句统一放在单个列表中，并按 ASCII 顺序排序。
- 禁止通配符导入。
- 禁止仅为缩短代码而使用无语义别名导入；`import ... as ...` 只能用于解决命名冲突或提升可读性。

### 2.4 顶级声明与类成员排序

- 文件内容必须集中在单个主题上。
- 类成员应按可解释的逻辑顺序排列，例如公开 API 到内部实现、状态到行为、生命周期到事件处理。
- 禁止把新成员习惯性追加到文件末尾；若顺序变化较大，必须保证 diff 可读并说明原因。
- 单个文件过长、公开声明过多或主题分散时，应优先拆分文件，而不是依赖注释分隔区域。

## 3. 格式化规则

### 3.1 缩进与空白

- 使用 4 个空格缩进，禁止使用 tab。
- 每个新块或类似块增加一级缩进；块结束后恢复上一级缩进。
- 每行只写一个语句，禁止使用分号。
- 避免水平对齐；变量名或类型名长度变化不应迫使无关行重新排版。
- 允许用空白行分隔逻辑段，但不鼓励连续多个空白行。
- 行尾注释 `//` 前后应保留空格；注释必须解释意图、约束或原因，不重复代码字面含义。

### 3.2 行宽与换行

- 普通代码行宽上限为 100 个字符。
- `package`、`import`、长 URL 和可直接复制执行的命令行注释可豁免行宽限制。
- 换行优先发生在较高语法层级，目标是让代码更清晰，而不是压缩为最少行数。
- 函数签名超过行宽时，每个参数独占一行；右括号和返回类型按 Kotlin/Android 样式独立排布。
- 属性初始化表达式过长时，在 `=` 后换行并增加一级缩进。

### 3.3 大括号与控制流

- 非空代码块使用 K&R 风格：左大括号位于行尾，右大括号独占一行。
- 除简单的单行 `if` 表达式和短 `when` 分支外，`if`、`for`、`when`、`do`、`while` 必须使用大括号。
- 多行条件的 `if` 或 `when` 必须使用大括号，条件续行缩进 4 个空格。
- `else`、`catch`、`finally` 和 `do-while` 的 `while` 与前一个右大括号位于同一行。
- 简短 `when` 分支可与条件同行；多行分支应使用代码块，并可用空白行分隔复杂分支。

### 3.4 空格规则

- 控制流关键字与左括号之间保留一个空格，例如 `if (...)`、`for (...)`。
- 函数声明、构造函数声明和函数调用的名称与左括号之间不留空格。
- 二元操作符两侧保留空格，但范围操作符 `..`、成员访问 `.`、安全调用 `?.`、成员引用 `::` 两侧不留空格。
- 逗号和冒号后保留一个空格。
- 类型声明中的冒号前不留空格；表示继承、接口实现或泛型约束的冒号前后按 Kotlin 规范保留必要空格。
- 可空标记 `?` 前不留空格。

### 3.5 尾随逗号

- 多行参数列表、集合字面量、枚举项、数据类属性和函数调用参数可使用尾随逗号。
- 同一项目或模块内必须保持一致；若启用尾随逗号，新增多行列表默认添加尾随逗号以降低 diff 噪声。
- 单行声明不得为了形式统一添加尾随逗号。

## 4. 命名规范

### 4.1 通用命名要求

- 标识符默认只使用 ASCII 字母、数字和必要下划线。
- 禁止使用无语义缩写、拼音缩写、项目内部黑话或需要口头解释的名称。
- 禁止使用特殊前缀或后缀，例如 `mName`、`sName`、`name_`、`kName`；后备属性除外。
- 缩写词按普通单词处理，避免 `URLParser` 与 `UrlParser` 在同一项目中混用。

### 4.2 Package、类型与函数

- package 名称全部小写，不使用下划线。
- 类、接口、枚举、注解、对象和类型别名使用 PascalCase。
- 类型名称通常使用名词或名词短语；接口可使用名词、名词短语、形容词或形容词短语。
- 函数名称使用 camelCase，通常使用动词或动词短语。
- 返回 `Unit` 的 `@Composable` 函数使用 PascalCase，并按组件或 UI 片段命名。
- Android 测试函数不得使用带空格的反引号名称；跨平台或纯 JVM 测试若使用反引号，必须确认构建和报告工具支持。

### 4.3 属性、变量与常量

- 普通属性、局部变量和参数使用 camelCase，通常使用名词或名词短语。
- 真正不可变且无副作用的顶级或 `object` 内部常量使用 UPPER_SNAKE_CASE。
- 标量常量必须优先使用 `const val`。
- 可观察状态可能改变的值不得使用常量命名，即使当前实现暂不修改。
- 后备属性使用下划线前缀，并与对外属性名称完全对应，例如 `_state` 与 `state`。

### 4.4 测试命名

- 测试类以被测类型或被测能力名称开头，以 `Test`、`IntegrationTest`、`UiTest` 等明确后缀结尾。
- Android 测试方法优先使用 camelCase；允许用下划线分隔场景、触发和期望结果。
- 测试名称必须表达行为，而不是表达实现细节。

### 4.5 禁止的空泛命名

以下命名只允许在具备明确领域语义时使用，否则禁止：

- `Util`、`Utils`
- `Helper`
- `Manager`
- `Processor`
- `Handler`
- `Wrapper`
- `Common`
- `Base`

若必须使用，应在命名中补足职责边界，例如 `DownloadRetryPolicy` 优于 `DownloadHelper`。

## 5. Kotlin 语言惯用法

### 5.1 不可变与空安全

- 初始化后不再变化的局部变量和属性必须使用 `val`。
- 只读集合使用 `List`、`Set`、`Map` 等不可变接口声明；只有确需修改时才暴露可变集合。
- 禁止用 `!!` 掩盖空安全设计问题；确需使用时必须靠近可信边界，并说明为什么该值不可能为空。
- 空值、缺省值和错误结果应通过类型、状态模型或显式分支表达，禁止依赖隐式约定。

### 5.2 函数与表达式

- 返回 `Unit` 的函数省略返回类型。
- 简单表达式函数可使用表达式函数体；公共 API、复杂表达式或影响可读性的场景应保留显式函数体或返回类型。
- 优先使用默认参数，避免为了少量参数组合创建重载函数。
- 多个相同原生类型参数或多个 `Boolean` 参数调用时，必须使用具名参数，除非上下文绝对清晰。
- 二元条件优先使用 `if`；三个或更多分支优先使用 `when`。
- 优先使用 `try`、`if`、`when` 的表达形式，但不得牺牲可读性。

### 5.3 Lambda 与集合操作

- 简短、非嵌套 lambda 可使用 `it`；嵌套 lambda 或语义不明显时必须显式命名参数。
- 避免在 lambda 中使用多个带标签返回；复杂控制流应拆成命名函数或匿名函数。
- 集合链式调用必须保持可读性；超过 3 步、包含副作用或调试困难时，应拆分为具名中间变量或函数。
- 禁止为了“函数式风格”隐藏副作用；有副作用的操作优先使用明确命名的函数或循环。

### 5.4 类型别名与扩展

- 重复出现且语义稳定的函数类型或复杂泛型可使用 `typealias`。
- 类型别名不得掩盖领域模型缺失或制造新的抽象层。
- 扩展函数必须与接收者或调用场景高度相关；不得把无关工具函数伪装成扩展。
- 公共扩展函数应谨慎命名，避免污染调用方自动补全和语义空间。

## 6. Android 专项编码规则

### 6.1 Android 组件

- Activity、Fragment、Service、BroadcastReceiver、ContentProvider 只承载 Android 生命周期、系统交互和页面装配职责，不承载业务规则或数据访问实现。
- ViewModel 负责 UI 状态、事件处理和 UseCase/Repository 编排，不直接持有 View、Context 或生命周期短于自身的对象。
- 需要 `Context` 时优先使用最小作用域；禁止把 Activity Context 长期保存在单例、Repository 或 DataSource 中。
- 资源访问、权限、Intent、Activity Result、系统服务和平台 SDK 调用必须通过清晰边界封装。

### 6.2 Compose

- 返回 `Unit` 的 `@Composable` 函数使用 PascalCase，并按 UI 组件命名。
- Composable 应保持无副作用渲染；副作用必须使用 Compose 明确的副作用 API，并说明生命周期语义。
- Composable 不直接访问 Repository、DataSource、数据库、网络接口或系统适配实现。
- 状态提升优先，UI 状态由 ViewModel 或调用方提供；可本地保存的纯 UI 瞬时状态必须明确恢复边界。
- Modifier 参数应保持稳定位置，通常作为第一个可选参数或遵循项目约定。

### 6.3 Views 与 XML

- Fragment/Activity 中的 ViewBinding 使用必须与生命周期对齐，Fragment 在 `onDestroyView` 释放绑定引用。
- Adapter、ViewHolder 和自定义 View 不承载业务规则；复杂状态转换下沉到 ViewModel、Presenter 或独立状态映射对象。
- 资源命名应与模块或能力可追溯，禁止无语义缩写。

### 6.4 协程与 Flow

- UI 层协程必须绑定到合适的生命周期作用域。
- Repository 和 DataSource 不得默认假设运行在主线程；阻塞 I/O 必须切换到合适调度器。
- Flow 暴露前应明确冷流/热流语义、错误传播、重试、去重和取消策略。
- 禁止使用 `GlobalScope` 承载业务任务；确需进程级任务必须通过明确的应用级作用域和取消策略治理。

## 7. 注释与 KDoc

- 注释用于解释意图、约束、边界、异常原因和非显然决策，不重复代码做了什么。
- 公共 API、跨模块契约、复杂状态机、非显然并发控制和平台适配边界必须补充 KDoc 或等价说明。
- 简短 KDoc 可写成单行；多行 KDoc 使用标准块格式。
- 通常避免机械使用 `@param` 和 `@return`；优先在自然语言中链接参数和返回语义。
- TODO 必须包含明确责任人、条件或跟踪项；禁止留下无期限 TODO。

## 8. 测试代码规范

- 测试代码同样遵守 Kotlin 命名、格式化和可读性规则。
- 单元测试优先围绕业务规则、状态转换、数据映射、Repository 协调和系统适配边界编写。
- 测试结构建议采用 Given/When/Then 或 Arrange/Act/Assert，但不得为形式增加噪声注释。
- Mock 只用于隔离外部依赖、时间、随机性、系统能力或昂贵 I/O；不得用 Mock 替代被测逻辑。
- 失败路径、边界条件、空数据、权限拒绝、超时、取消和恢复路径必须按风险覆盖。
- Android 仪器测试、Compose UI Test、Espresso、Robolectric 或 Maestro 的选择由项目测试策略决定，并需说明覆盖边界。

## 9. Gradle Kotlin DSL 规范

- Gradle 脚本优先使用 Kotlin DSL。
- 依赖版本通过 Version Catalog 或统一版本管理入口维护，模块脚本不得直接写死新依赖版本。
- 插件、依赖、Android 配置和任务配置应按稳定顺序组织，避免无关 diff。
- 自定义构建逻辑达到复用边界时，应收敛到 convention plugin 或 build logic 模块。
- 构建脚本中的字符串常量、任务名和路径必须可追溯；禁止散落魔法字符串。

## 10. 工具化与质量闸门

- 项目应配置 Android Studio formatter、ktlint、detekt 或等价工具，并使规则与本宪章一致。
- 提交前至少运行与改动匹配的格式化、静态检查和测试命令。
- 自动格式化不得掩盖大范围无关改动；格式化提交应与行为变更分离。
- 新增规则必须说明是否可自动检查；不可自动检查的规则必须进入评审清单。

## 11. 评审检查清单

- 文件名、package、import 和文件结构是否符合规范。
- 是否存在通配符导入、无语义别名导入或未使用 import。
- 命名是否表达职责，是否存在空泛 `Util`、`Helper`、`Manager` 等命名。
- 是否遵守 4 空格缩进、100 字符行宽、K&R 大括号、无分号和空白规则。
- 是否优先使用 `val`、不可变集合、默认参数和清晰表达式。
- 是否存在不必要的 `!!`、可变状态暴露、复杂 lambda、隐藏副作用或过度链式调用。
- Android 组件、Composable、ViewModel、Repository 和系统能力边界是否清晰。
- 协程、Flow、生命周期和调度器使用是否安全。
- 注释是否解释意图和约束，而不是复述代码。
- 测试命名、测试结构和失败路径覆盖是否满足变更风险。
- Gradle Kotlin DSL 是否通过 Version Catalog 或统一入口管理依赖版本。

## 参考来源

- Google Android Kotlin Style Guide: https://developer.android.google.cn/kotlin/style-guide?hl=zh-cn
- Kotlin 官方编码规范中文版: https://book.kotlincn.net/text/coding-conventions.html
