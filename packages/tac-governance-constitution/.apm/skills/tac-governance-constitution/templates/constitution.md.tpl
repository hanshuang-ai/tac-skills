# {{项目名称}} 项目宪章

## Core Principles

### I. 主宪章统领
项目治理必须采用"1 个主宪章 + 多个专项规范"的层级结构。主宪章只定义全项目统一的
非协商规则、流程入口、优先级关系和合规要求，不承载特定工作类型的细节清单。
专项规范用于承载某一类工作的强约束，当前至少包含工程规范、编码规范、功能点分析规范和研发流程规范。
任何新增专项规范都必须在主宪章中登记名称、适用范围、优先级与维护位置。
适用专项规范的判定不得依赖作者主观选择，而必须按变更类型强制触发：新增模块、依赖选型、网络通信、
存储、分层调整等变更必须引用工程规范；{{源码语言}}源码、测试代码、{{构建脚本类型}}变更必须引用编码规范；
需求边界、接口、数据模型、场景与成功标准变更必须引用功能点分析规范；提交、推送、送审变更必须引用研发流程规范。
若命中触发条件却未引用对应专项规范，视为不合规，不允许进入实现或评审。
理由：不同角色会使用 Speckit 模式协作，但不一定使用同一代理工具，治理结构必须稳定、
清晰且与具体工具解耦。

### II. {{文档语言}}文档优先
所有面向协作与评审的文档必须默认使用{{文档语言}}，包括 `README.md`、`PROJECT.md`、
文档、需求、设计、评审记录、操作手册与 Spec Kit 产物。命令、路径、类名、接口名、
第三方术语和必须保留的协议字段可以使用{{对侧语言}}。任何新增模板与流程文档都必须延续这一规则。
理由：本项目以跨角色协作为主，{{文档语言}}是降低沟通成本、缩短评审链路的默认语言。

### III. {{场景}}场景优先
{{场景约束描述}}

### IV. 规格驱动交付
任何新增功能、明显行为变更或跨模块调整，必须先经过 Spec Kit 流程中的规格、规划和任务拆解，
再进入实现阶段。`spec.md` 必须说明用户场景、边界条件、可验证需求和成功标准；
`plan.md` 必须说明技术背景、结构决策、专项规范引用和宪章检查；`tasks.md` 必须按用户故事组织并可执行。
当需求涉及系统设计或功能点分析时，必须同时满足对应专项规范。
理由：本项目是全链路 AI 编程试点，需求到实现必须可追踪、可复审、可复用。

### V. 设计原则优先
所有系统设计、模块划分、接口契约和关键实现都必须优先遵循统一的设计原则，
不得以临时方便、个人偏好或工具输出替代设计判断。主宪章要求默认遵循仓库登记的七大设计原则，
其具体清单、{{技术栈}}化落点和评审细则由工程规范专项规范维护。设计模式属于受控手段，
只有在能降低复杂度、提升可维护性或稳定边界时才应采用，并必须说明为何需要该模式、
为何不采用更简单方案以及引入后的边界影响。
理由：设计原则决定长期可维护性，设计模式只是实现手段；主宪章必须先约束判断标准，
再允许专项规范细化具体落地。

### VI. 质量闸门先行
每次变更在完成前必须至少执行与改动匹配的验证。业务逻辑修改必须补充或更新单元测试；
涉及{{平台敏感面}}时，必须评估{{对应集成/仪器测试}}；提交前至少应运行 `{{构建命令}}`、
`{{单元测试命令}}` 或 `{{静态检查命令}}` 中与改动直接相关的命令，并在交付说明中记录结果和未执行原因。
理由：AI 生成与协作式开发提高了产出速度，也放大了回归风险，质量闸门必须前置且可审计。

### VII. 测试驱动开发
涉及业务逻辑的新增或修改，必须遵循"先写测试、再写实现、最后重构"的 TDD 节奏。
测试必须先于实现代码编写并确认失败（Red），再编写最少量的实现代码使测试通过（Green），
最后在测试保护下进行重构（Refactor）。纯删除、纯文档和纯配置变更可豁免 TDD 流程，
但必须在交付说明中注明豁免原因。任务拆解阶段（`tasks.md`）必须为每个涉及业务逻辑的
用户故事生成测试任务，且测试任务排在实现任务之前。评审时必须检查测试是否先于实现提交，
以及测试是否覆盖了变更的核心路径、边界条件和异常场景。
理由：AI 编程容易产出"编译通过但逻辑未验证"的代码，TDD 通过测试先行强制验证每一步
实现的正确性，同时为后续重构提供安全网，防止回归。

### VIII. 思考先行
任何编码动作开始前，必须先显化假设、暴露权衡、消除歧义，禁止"伪自信"地默默落地。
- 必须显式陈述本次变更所依赖的假设；存在不确定时必须先发问，不得猜测。
- 若需求或上下文存在多种合理解读，必须列出所有可能解读并请用户裁决，不得默默选择一种。
- 若存在更简单的替代方案，必须主动提出；必要时基于事实反驳当前提议，而非附和。
- 若任何信息不清晰，必须停下并具体指出"哪里不清楚、需要补充什么"，禁止以"继续推进"掩饰困惑。
适用范围：所有由 AI 工具发起或推进的编码、设计、修复与重构任务；对琐碎的格式化、重命名等
低风险动作可凭判断豁免。
理由：AI 编程最大的回归来源是基于错误假设产出大量"看似合理但与意图不符"的代码，
显化假设是降低误工与返工成本的最低代价手段。

### IX. 极简实现
仅交付解决当前问题所需的最小代码量，禁止任何非请求范围的扩展。
- 不实现未被要求的功能；不为一次性代码引入抽象层、配置项或扩展点；
- 不添加未请求的"灵活性""可配置性"或"通用化"；
- 不为不可能发生的场景编写防御性错误处理；
- 若 200 行可由 50 行等价实现，必须重写为更短的版本；
- 自查标准为"资深工程师是否会判定此处过度复杂"——若答案为是，必须简化后再提交。
适用范围：所有{{源码语言}}源码、{{构建脚本类型}}、文档与脚手架变更；与本原则冲突时，第 V 条
"设计原则优先"在"是否需要抽象"的判定上具有同等权重，由 plan.md 显式说明取舍。
理由：AI 容易输出过度通用化、过度工程化的代码，抬高了长期维护成本却未解决用户真实问题。

### X. 精准修改
只修改必须修改的内容，只清理由本次变更产生的副产物，禁止扩大改动范围。
- 不"顺手"优化邻近代码、注释或格式；不重构未损坏的代码；
- 即便个人风格不同，也必须沿用现有代码风格；
- 若发现无关的死代码，可在交付说明中提示，但不得在本次变更中删除；
- 当本次修改造成了代码孤立时，必须移除由本次修改新增的未使用 import/变量/函数；
- 不得移除预先存在的死代码，除非用户显式要求。
判定标准：变更后每一行 diff 都应可直接追溯到用户请求或宪章触发条件，否则视为越界变更。
理由：扩大变更范围会污染评审、放大回归风险、令历史难以追溯，与"规格驱动交付"的可追踪
要求直接冲突。

### XI. 目标驱动执行
所有任务必须转化为可验证的成功标准后再进入实现，禁止以"让它工作"等弱标准开工。
- 必须把模糊请求改写为可验证目标：
  - "添加校验" → "为非法输入编写测试并使其先红后绿"；
  - "修复 Bug" → "先写出能稳定复现该 Bug 的测试，再让其通过"；
  - "重构 X" → "重构前后单元测试与必要的集成测试均必须通过"。
- 多步任务必须先列出简明计划，且每一步都给出对应的验证手段，例如：
  1. [步骤] → 验证：[检查项]
  2. [步骤] → 验证：[检查项]
  3. [步骤] → 验证：[检查项]
- 缺乏可验证成功标准时，必须先澄清或补全标准，再开始实现。
理由：可验证的成功标准让 AI 能在最小监督下闭环并自我纠偏；弱标准会迫使协作者反复回头
澄清，造成上下文丢失与返工，并削弱第 VI 条质量闸门和第 VII 条 TDD 的执行效果。

## 专项规范清单

- 输入质量专项规范：[`input-quality-constitution.md`](./input-quality-constitution.md)
  适用于 AI 工具接收功能开发与 Bug 修复请求时的输入完整性闸门、追问模板、字段层质量约束与评审追溯，所有进入规格阶段的请求必须先满足本规范。
- 工程规范专项规范：[`{{工程规范文件名}}`](./{{工程规范文件名}})
  适用于 {{技术栈}} 架构、分层、模块、依赖、资源、编码与团队协作相关决策。
- 编码规范专项规范：[`{{编码规范文件名}}`](./{{编码规范文件名}})
  适用于 {{源码语言}} 源码、测试代码、{{构建脚本类型}} 与代码评审中的命名、格式化、布局和惯用法约束。
- 文档治理专项规范：[`document-governance-constitution.md`](./document-governance-constitution.md)
  适用于文档分类、目录归属、内容规范、权威层级、AI 输入读取规则与文档生命周期治理。
- 若主宪章与专项规范冲突，以主宪章为准；

## 技术与合规边界

- 包名空间：`{{包名空间}}`
- 主要构建工具与版本约束（如 Android Gradle Plugin / Xcode / Node / Go 版本要求）由工程规范专项规范维护。
- 第三方依赖必须显式登记来源、版本与合规风险；与公司安全/合规策略冲突的依赖一律禁止引入。

## 研发流程与评审要求

{{评审流程描述}}

## Governance

本宪章高于项目内其他流程说明；当 README、模板、局部规范与本宪章冲突时，以本宪章为准。
`PROJECT.md`、README、操作指南和索引文档只允许作为协作入口、执行索引和风险提示，
不得独立定义高于主宪章或专项规范的强约束。任何会被 Hook、Gerrit、模板或专项规范频繁调整的细则，
都必须收口到对应专项规范，由入口文件通过"摘要 + 跳转"方式引用。
宪章修订必须同步更新相关模板与运行说明，并在文件顶部的 Sync Impact Report 中记录影响范围。
新增或调整专项规范时，必须同步说明适用角色、输入输出、触发条件与主宪章的关系。
版本号遵循语义化规则：新增原则或实质扩展记为 MINOR，删除或重定义原则记为 MAJOR，
措辞澄清与非语义性调整记为 PATCH。所有规格、计划、任务与代码评审都必须显式检查宪章符合性。
