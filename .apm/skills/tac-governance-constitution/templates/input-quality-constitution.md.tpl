# 输入质量专项宪章

## 适用范围

本宪章适用于所有 AI 工具接收的工作请求输入，包括但不限于 Claude Code、GitHub Copilot、Gemini CLI、Cursor 及其他支持上下文注入的 AI 编程工具。

本宪章是项目主宪章之下的输入质量前置闸门，与工程规范、编码规范、文档治理等专项宪章并列。具体业务字段、产品形态、单一工具的 Hook 实现细节、脚本路径与工具登记应写入项目级 `PROJECT.md`、运行说明或工具适配文档，不得写入本通用专项宪章。

## 与主宪章的关系

- 主宪章高于本宪章。
- 本宪章是主宪章「规格驱动交付」原则的输入质量前置闸门。
- 主宪章要求「先经过规格、规划和任务拆解再进入实现」，本宪章进一步要求「输入本身必须结构化才能进入规格阶段」。
- 若本宪章与主宪章冲突，以主宪章为准；若项目级运行说明与本宪章冲突，业务事实以项目级说明为准，通用输入约束以本宪章为准，并在评审中记录裁剪原因。

## 1. 输入分类与最低要求

### 1.1 功能开发 / 行为变更

触发关键词：新增、添加、实现、开发、创建、修改行为、重构、优化、调整、对齐、替换、迁移、接入、对接、改为、改成、换成、支持、升级、适配。

**必填项（前 6 项必填，第 7 项可选）：**

| 字段 | 必填 | 对应模板 | 说明 |
| ---- | ---- | -------- | ---- |
| 功能 | 是 | 【功能】 | 一句话描述功能目标 |
| 场景 | 是 | 【场景】 | 用户操作路径，从哪里进入、做什么操作、看到什么结果 |
| 输入 | 是 | 【输入】 | 接收什么数据、数据来源 |
| 输出 | 是 | 【输出】 | 产出什么结果、如何展示 |
| 约束 | 是 | 【约束】 | 技术栈 / 性能 / 兼容性 / 安全 / 目标环境要求 |
| 验收标准 | 是 | 【验收标准】 | AC 编号 / 设计文档路径+章节 / 内联 Given-When-Then / 内联通用判据 / 复用既有测试基线（详细规则见 §6） |
| 参考 | 否 | 【参考】 | 相关设计文档、API 文档、截图路径 |

**缺少 2 项及以上必填字段时，AI 必须追问后再执行，不得凭推测开始实现。**

### 1.2 Bug 修复

触发关键词：修复、bug、崩溃、crash、报错、异常、不生效、闪退、ANR、无响应、白屏、黑屏、不显示、显示异常、错位、溢出、重叠、丢失、消失、卡住、卡顿、失败。

**必填项（前 3 项必填，后 2 项可选）：**

| 字段 | 必填 | 对应模板 | 说明 |
| ---- | ---- | -------- | ---- |
| 现象 | 是 | 【现象】 | 什么操作导致什么异常结果 |
| 预期 | 是 | 【预期】 | 正常情况下应该是什么结果 |
| 复现 | 是 | 【复现】 | 操作步骤 1-2-3 |
| 日志 | 否 | 【日志】 | 关键堆栈或错误日志 |
| 影响 | 否 | 【影响】 | 影响范围、影响版本 |

**缺少 2 项及以上必填字段时，AI 必须追问后再执行。**

### 1.3 豁免场景（可直接执行）

- 纯文档 / 规范更新。
- 代码格式化 / lint 修复。
- 用户已指定文件路径 + 具体修改内容。
- 用户明确声明「跳过输入检查」或「直接做」。
- Spec Kit 命令调用（已有自己的结构化流程）。

## 2. 追问模板

### 2.1 功能开发追问模板

当用户输入缺少必填项时，输出以下完整模板要求补充：

```
【功能】：<一句话描述功能目标>
【场景】：<用户操作路径，从哪里进入、做什么操作、看到什么结果>
【输入】：<接收什么数据，数据来源>
【输出】：<产出什么结果，如何展示>
【约束】：<技术栈/性能/兼容性/安全/目标环境要求>
【验收标准】：<AC 编号列表 / 设计文档路径+章节 / 内联 Given-When-Then / 内联通用判据 / 复用既有测试基线>（详见 §6）
【参考】：<相关设计文档、API 文档、截图路径>（可选）
```

### 2.2 Bug 修复追问模板

```
【现象】：<什么操作导致什么异常结果>
【预期】：<正常情况下应该是什么结果>
【复现】：<操作步骤 1-2-3>
【日志】：<关键堆栈或错误日志>（可选）
【影响】：<影响范围、影响版本>（可选）
```

## 3. 执行要求

- 当 AI 工具接收到功能开发或 Bug 修复请求时，必须首先按本宪章检查输入完整性。
- 输入不完整时，AI 必须输出对应追问模板，等待用户补充后再继续。
- 禁止在输入不完整的情况下凭推测开始实现。
- 本宪章通过 `PROJECT.md` 的场景映射引用，所有 AI 工具均应遵循。
- 支持 Hook 的工具可通过项目级输入质量校验脚本自动化校验；具体脚本名称、路径与启用方式由项目级 `PROJECT.md` 或运行说明登记，不在本通用宪章固化。
- 支持 Skill 的工具可通过功能开发输入收集 skill（如 `tac-feature`）或 Bug 修复输入收集 skill（如 `tac-bugfix`）入口强制收集；具体 skill 名称以项目级 skill 注册表为准。

## 4. 评审要求

- 在 Spec Kit 流程的 Review Gate 1（审查 `spec.md`）时，评审人必须检查 spec 的用户故事是否可追溯到结构化输入。
- 若 spec 中存在无输入来源的功能点，视为不合规，不得进入实现阶段。
- 评审记录中必须能反向定位到原始结构化输入（追问模板填充结果或等效结构化记录）。

## 5. 输入来源追溯

本宪章不绑定特定项目的文档路径；项目级实现细节由以下来源补充：

- 项目级 AI 编程治理材料（如团队对外的 AI 编程工程实践指南、白皮书、培训材料）：承载团队输入治理的完整方案与背景。
- 项目级主宪章 `.specify/memory/constitution.md`：定义「规格驱动交付」等上层原则。
- 项目级功能点分析专项宪章（若已生成）：定义功能点的语义边界、最小完备字段与跨阶段一致性要求。
- 项目级运行说明（`PROJECT.md`、`README.md`）：登记本宪章在当前项目中的具体接入方式、Hook 脚本路径与 Skill 入口名称。

若以上来源之间出现冲突，以项目主宪章优先，其次以本专项宪章为准；项目级运行说明只承载接入方式与路径登记，不得反向覆盖本宪章的结构化要求。

## 6. 验收标准字段质量要求

【验收标准】字段必须满足以下五种形式之一，否则视为不合规须追问。本节是字段层质量约束的权威源，所有 AI 工具（Skill / Hook / 插件）实施时不得放宽。

| 形式 | 合规示例 | 校验规则 |
| ---- | -------- | -------- |
| AC 编号列表 | `AC-HOM-FN-01..05, AC-DL-RU-12` | 至少 1 个匹配 `AC-[A-Z]+-[A-Z]+-\d+` 的编号；多编号用逗号或区间 `..` 分隔 |
| 设计文档路径 + 章节 | `doc/design/<slug>/软件验收标准.md §3.2` | 路径可读 + 章节锚点（`§数字(.数字)*` 或 `#anchor` 或显式行号） |
| 内联 Given-When-Then | `Given 已登录用户在首页 / When 点击下载按钮 / Then 5 秒内进入下载列表` | 至少 1 组完整三元组，三段缺一不可 |
| 内联通用判据 | `调用方应能取消下载并释放占用磁盘空间，UI 不残留 loading 态` | 至少 1 条含具体动作 + 可观察期望结果的判据 |
| 复用既有测试基线 | `无独立验收文档；复用 feature-download/.../DownloadCoordinatorTest 既有测试基线` | 显式声明「无独立验收文档」 + 至少 1 个现有测试文件路径（路径可读） |

**典型不合规示例（AI 须追问）：**

- 空字符串、单纯「无」、单纯「见验收标准文档」（未给具体路径与章节）。
- 占位语：`TODO` / `TBD` / `待定` / `稍后补充` / `按通用规范`（未点名规范）。
- AC 编号格式错误：`AC123` / `acceptance-1`。
- Given/When/Then 残缺：仅 `Given...` 或仅 `Then...`。
- 通用判据无具体动作或无可观察结果：`需要测试通过` / `性能要好`。

**追问模板（字段层）：**

```
【验收标准】字段当前内容 <具体内容> 不满足质量约束（<失败原因>）。请按以下任一形式补充：
- AC 编号列表（如 AC-HOM-FN-01..05）
- 设计文档路径+章节（如 doc/design/<slug>/软件验收标准.md §3.2）
- 内联 Given/When/Then 至少 1 组
- 内联通用判据至少 1 条（含具体动作与期望结果）
- 显式「无独立验收文档」+ 复用的既有测试文件路径
```
