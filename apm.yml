name: apm
version: 1.0.0
description: APM project for apm
author: lcy
# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: claude, codex, copilot, cursor, gemini, kiro, opencode, windsurf
targets:
- claude
dependencies:
  apm: []
  mcp: []
includes: auto
scripts: {}
