# {{产品/Feature 名称}} 软件验收标准

> 派生自：
> - `persistent-assets/design/_baseline/02-详细设计.md`
> - `persistent-assets/design/_baseline/01-概要设计.md`
> - `persistent-assets/design/_baseline/00-业务与领域设计.md`
> - `{{PRD 文件相对路径}}`
>
> 文档版本：v1.0 · {{YYYY-MM-DD}}
> 演进自（维护模式必填，首次跑写"无"）：{{上一版 feature-slug @ commit hash}}

## 0. 变更记录

> 首次跑：本节内容为"首次落盘，无变更记录"。维护模式：每次维护跑必须追加新行；条目数与 M1 影响矩阵 1:1。新增/修改/删除用例时同步在 §9 派生与追溯总表中加 changelog 引用行。

| 日期 | 变化来源（PRD 章节 / 触发原因） | 类型（澄清/修改/新增/废弃/重命名） | 影响章节 | 简述 |
|------|------------------------------|----------------------------------|---------|------|
| {{YYYY-MM-DD}} | {{PRD §X.X}} | {{...}} | {{§N / §M}} | {{...}} |

## 1. 验收范围

### 1.1 In-Scope（必须验收覆盖）

| # | PRD 项 | 对应验收章节 |
|---|-------|-------------|
| 1 | {{PRD §X "用户登录"}} | §3.1 / §4.1 |
| 2 | {{...}} | {{...}} |

### 1.2 Out-of-Scope（不在本次验收）

- {{...}}

## 2. 用例编号与标签规则

- 编号：`AC-<MODULE>-<TYPE>-<NN>`
  - MODULE：来自 DLD 模块名（短码）
  - TYPE：`FN`(功能) / `ST`(状态机) / `RU`(业务规则) / `PF`(性能) / `SC`(安全) / `OB`(可观测) / `CO`(兼容性) / `RG`(回归)
  - NN：两位顺序号
- 标签：`@happy`、`@error:E1001`、`@nfr:latency`、`@regression:<issue-id>` 等

## 3. 功能验收

### 3.1 模块 {{M1}}

#### AC-M1-FN-01 {{接口 X happy path}}

| 字段 | 内容 |
|------|------|
| 被测对象 | DLD §4.1.3 接口 1 |
| Given | {{前置：用户已登录、库存=10}} |
| When | {{执行：调用 `methodName(args)`}} |
| Then | {{断言 1：返回值字段 X = Y / 状态变成 Z / 持久化 W 表新增 1 行}} |
| 通过判据 | {{量化：HTTP 200 且 body.code=0 且 P99 < 200ms}} |
| 执行方式 | 自动化：`./gradlew :feature-x:testDebugUnitTest --tests "*.M1Test#xxx"` |
| 追溯 | DLD §4.1.3 / Stage1 R3 |

#### AC-M1-FN-02 {{接口 X 错误码 E1001}}

| 字段 | 内容 |
|------|------|
| 被测对象 | DLD §4.1.3 接口 1 |
| Given | {{触发 E1001 的前置：参数缺失/越权/数据冲突}} |
| When | {{...}} |
| Then | 返回错误码 `E1001`，message 含 `<关键字>` |
| 通过判据 | 异常类型 = `XxxException`，errorCode = `E1001` |
| 执行方式 | 自动化：`...` |
| 追溯 | DLD §4.1.3 错误码 E1001 |

#### AC-M1-FN-NN ...

### 3.2 模块 {{M2}}

> 重复结构

## 4. 状态机验收

### 4.1 {{聚合根 A}} 状态机

#### AC-M1-ST-01 合法转移：{{State1}} → {{State2}}

| 字段 | 内容 |
|------|------|
| 被测转移 | DLD §4.1.5 状态机 |
| Given | 当前状态 = State1 |
| When | 触发事件 = `event1` |
| Then | 状态变为 State2，触发 entry 动作 `<...>` |
| 通过判据 | 持久化的 status 字段 = `STATE2` |
| 执行方式 | {{...}} |
| 追溯 | DLD §4.1.5 |

#### AC-M1-ST-02 非法转移：{{State1}} ✗← `event2`

| 字段 | 内容 |
|------|------|
| 被测转移 | DLD §4.1.5 中 State1 不接受 event2 |
| Given | 当前状态 = State1 |
| When | 触发事件 = `event2` |
| Then | 返回错误码 `E1003`，状态保持 State1 |
| 通过判据 | 异常类型 + status 字段未变 |
| 执行方式 | {{...}} |
| 追溯 | DLD §4.1.5 |

## 5. 业务规则验收

### AC-M1-RU-01 业务规则 R1：{{规则描述}}

| 字段 | 内容 |
|------|------|
| 关联规则 | Stage 1 §8 R1 |
| Given | {{满足规则前置}} |
| When | {{触发}} |
| Then | {{断言：拒绝/通过、状态变化}} |
| 通过判据 | {{量化}} |
| 执行方式 | {{...}} |

## 6. 非功能验收

### 6.1 性能

#### AC-PF-01 {{NFR：P99 ≤ 200ms}}

| 字段 | 内容 |
|------|------|
| 关联 NFR | HLD §11 NFR1 |
| 负载条件 | {{X QPS / Y 并发 / Z 数据量 / 持续 N 分钟}} |
| 度量方式 | 监控指标 `<metric_name>`，P99 over 5min window |
| 通过判据 | P99 ≤ 200ms（容许 1 次 burst > 200ms 但 ≤ 300ms） |
| 执行方式 | {{压测脚本 `scripts/load-test.sh` / Grafana 看板 `<url>`}} |
| 追溯 | HLD §11 |

### 6.2 安全

| 用例 ID | 检查项 | 验收方法 | 通过判据 |
|---------|-------|---------|---------|
| AC-SC-01 | 未授权访问被拒绝 | 不带 token 调用受保护接口 | 返回 401 且不泄漏内部错误 |
| AC-SC-02 | SQL 注入防护 | 输入 `' or '1'='1` | 输入校验拦截，记录审计日志 |
| AC-SC-NN | ... | ... | ... |

### 6.3 可观测性

| 用例 ID | 关键路径 | 应触发的埋点 | 通过判据 |
|---------|---------|-------------|---------|
| AC-OB-01 | {{接口 1 调用}} | 日志 `req_id`, 指标 `m1_call_count`, Trace span `m1.call` | 三者全部出现且字段完整 |
| AC-OB-NN | ... | ... | ... |

### 6.4 兼容性

| 用例 ID | 维度 | 范围 | 通过判据 |
|---------|------|------|---------|
| AC-CO-01 | Android 版本 | API 24-34 | 全部测试通过 |
| AC-CO-02 | 设备 | {{车机机型 / 屏幕分辨率范围}} | UI 不错位、功能可用 |
| AC-CO-NN | ... | ... | ... |

## 7. 回归基线

> 已修复缺陷"应不再复现"用例。如无历史 issue，显式写"无历史回归基线，本 feature 为首次实现"。

| 用例 ID | 关联 issue | 描述 | 通过判据 |
|---------|-----------|------|---------|
| AC-RG-01 | {{ISSUE-XXX}} | {{...}} | {{...}} |

## 8. 验收执行计划

| 阶段 | 用例集 | 执行频率 | 责任 |
|------|-------|---------|------|
| 模块单测 | AC-Mx-FN-* / AC-Mx-ST-* / AC-Mx-RU-* 中可单测的 | 每次 commit + CI | 开发 |
| 集成测试 | 跨模块功能用例 | 每次合入主干 | 开发 |
| 性能测试 | AC-PF-* | 每周 + 重大变更 | 测试/开发 |
| 安全审计 | AC-SC-* | 每月 + 上线前 | 安全/开发 |
| 兼容性扫测 | AC-CO-* | 上线前 | 测试 |
| 监控验证 | AC-OB-* | 上线后 24h | 开发/SRE |

## 9. 派生与追溯总表

| 上游元素 | 验收用例编号 |
|---------|-------------|
| PRD §1.1 | AC-M1-FN-01 |
| PRD §1.2 | AC-M1-FN-02, AC-M2-FN-03 |
| ... | ... |
| DLD §4.1.3 接口 1 | AC-M1-FN-01, AC-M1-FN-02 |
| DLD §4.1.5 非法转移 (State1, event2) | AC-M1-ST-02 |
| ... | ... |
| Stage 1 §8 R1 | AC-M1-RU-01 |
| HLD §11 NFR1 | AC-PF-01 |
